---
applyTo: "**/*.py"
description: Python coding standards for the project.
---

- We use `django` for the application, so follow Django's coding standards.
- Tests should be written using `pytest`.
- Loading loading environment can be tricky, so instead of running commands yourself, you will ask the user to run them.
